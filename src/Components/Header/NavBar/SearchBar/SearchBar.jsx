import React from "react";

const SearchBar = props => {
  return (
    <div>
      <h2>RECHERCHER</h2>
      <form>
        <input size="60" />
        <button>Search</button>
      </form>
    </div>
  );
};

export default SearchBar;
