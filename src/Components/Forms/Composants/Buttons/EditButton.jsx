import React from "react";
import classes from "../../form.css";

const EditButton = () => {
  return (
    <button className={classes.editButton}>Valider les modifications</button>
  );
};

export default EditButton;
